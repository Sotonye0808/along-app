# UI Components

This directory contains reusable UI components that are used across the application.

These components are typically wrapped Ant Design components with custom styling or generic components like buttons, inputs, cards, etc.

## Structure

- Each component should have its own directory
- Component files should be named in PascalCase
- Export components from index files for cleaner imports
